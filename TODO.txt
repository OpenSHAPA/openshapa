- Code cleanup/documentation (around plugins)
- Check to remove DataStore
- Simplify the VideoController (removed, Identifier interface, IDController, ID->Identifier, DataStorage)
- Simplify the ClockTimer

- Add a class that uses native control for playback speed
- Add playback speed at 0.5x (and others) preferably through filter in ffmpeg
- Replace Swing Application Framework with JavaFX